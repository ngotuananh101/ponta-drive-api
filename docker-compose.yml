services:
  goravel:
    build:
      context: .
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${APP_HOST}:${APP_PORT}:${APP_PORT}"
    networks:
      - bridge
    restart: always

networks:
  bridge:
    external: true
